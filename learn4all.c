#include <stdio.h>
#include <stdlib.h>

int readFile()
{
    /*char line[30];
    FILE *myFile = fopen("black.txt", "r");

    fprintf(myFile, "\n\tnnvnk");
    for(int i=0; i<2; i++){
        fgets(line, 30, myFile);
        printf("%s\n", line);
    }

    fclose(myFile);*/
}

int main()
{
    if(6) printf("\#");
    return 0;
}
